#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    new_matrix = []
    for a in matrix:
        new_a = []
        new_matrix.append(new_a)
        for b in a:
            new_iaappend(b ** 2)
    return new_matrix
